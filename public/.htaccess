<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # API routes — pass through to PHP
  RewriteRule ^api/ - [L]

  # SPA fallback for client-side routing
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
